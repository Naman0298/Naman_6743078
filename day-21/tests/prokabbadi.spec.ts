import { test, expect } from '@playwright/test';

test('Pro kabbadi recent match details', async ({ page, context }) => {
    // await page.goto('https://www.prokabaddi.com/');
    // await expect(page).toHaveTitle(/Pro Kabaddi/);
    // page.locator('a[data-parameter="homepage_fixtures_more_link"]').click()
    // await page.waitForLoadState();

    // const latestMatch = page.locator(".match-row").first();

    // const team1 = await latestMatch.locator(".team-name").nth(0).textContent();
    // const team2 = await latestMatch.locator(".team-name").nth(1).textContent();
    // const score1 = await latestMatch.locator(".score").nth(0).textContent();
    // const score2 = await latestMatch.locator(".score").nth(1).textContent();

    // console.log(`Latest Match: ${team1} ${score1} - ${score2} ${team2}`);

});

test('Pro kabbadi recccent match details', async ({ page, context }) => {


});
test('Pro kabbadi reccent match details', async ({ page, context }) => {


});
test('Pro kabbacdi recent match details', async ({ page, context }) => {


});